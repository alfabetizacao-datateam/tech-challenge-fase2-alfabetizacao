# Nucleo do pipeline (Bronze/Silver/Gold, testes, deploy GCP)
# Levantado por grep real dos imports em src/ e tests/ - nao e pip freeze.
#
# setuptools<81: Python 3.12+ removeu 'distutils' da stdlib. pyspark==3.5.0
# usa distutils.version.LooseVersion internamente (toPandas() quebra sem isso).
# setuptools <81 ainda vendoriza um shim de distutils que cobre o buraco.
# Sem isso: "ModuleNotFoundError: No module named 'distutils'" em qualquer
# .toPandas() (ex: src/features/02_imputar_metas_knn.py).
setuptools<81
pyspark==3.5.0
pandas==2.3.3
numpy==2.4.4
scikit-learn==1.8.0
requests==2.33.1
pytest==9.0.3
google-cloud-storage==2.14.0
google-cloud-bigquery==3.17.2

# Dependencias adicionais por feature (nao vem aqui - evita instalar o que nao precisa):
# - requirements-ai.txt (Task 12: RAG/Streamlit) - chromadb, google-generativeai, sentence-transformers, streamlit
# - great-expectations (Task 8: DQ avancado, opcional - o script cai em fallback manual sem ele)
